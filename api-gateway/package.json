{
  "name": "api-gateway",
  "version": "1.0.0",
  "description": "API Gateway for E-commerce microservices",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions:api-gateway",
    "deploy": "npm run build && firebase deploy --only functions:api-gateway"
  },
  "dependencies": {
    "express": "^4.19.2",
    "firebase-admin": "^12.4.0",
    "firebase-functions": "^5.1.1",
    "axios": "^1.7.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/cors": "^2.8.17",
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  }
}

